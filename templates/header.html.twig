<!DOCTYPE html>
{% if app.user %}
<div class="mb-3">
    <p style="color:blue"> You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
    </p>
</div>
{% else %}
    <div class="mb-3">
        <a href="{{ path('app_login') }}">Login</a>
    </div>
{% endif %}

           {# {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="{{ path('app_logout') }}">Logout</a>
            {% else %}
                <a href="{{ path('app_login') }}">Login</a>
            {% endif %}  #}